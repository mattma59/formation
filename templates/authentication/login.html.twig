{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}

    {% if errors %}

        {{ errors.message }}

    {% endif %}

    {{ form_start(form, {attr: {'novalidate' : 'novalidate'}}) }}

    {{ form_widget(form) }}

    {{ form_end(form) }}


{% endblock %}
